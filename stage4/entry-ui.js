/* Dingel Hafizia App — Complete Income/Expense Entry UI */
(function(){
  const CATEGORY_KEY='dh_transaction_categories';
  const defaults={
    Income:['Student Fees','Admission Fee','Donation','Zakat','Fitra','Other Income'],
    Expense:['Teacher Salary','Staff Salary','Food & Kitchen','Rent','Electricity','Water','Gas','Internet & Phone','Education & Stationery','Books & Quran','Uniform & Clothing','Student Welfare','Medical','Cleaning & Hygiene','Repairs & Maintenance','Furniture & Equipment','Transport','Office Expenses','Program & Events','Religious Activities','Bank & Payment Charges','Government & Registration','Other Expense']
  };
  const clean=(arr,fallback)=>{const a=Array.isArray(arr)?arr:[];const out=a.filter(x=>typeof x==='string'&&x.trim()&&x.trim().toLowerCase()!=='normal').map(x=>x.trim());return out.length?out:fallback.slice()};
  const categoryData=()=>{try{const v=JSON.parse(localStorage.getItem(CATEGORY_KEY)||'null');const data={Income:clean(v?.Income,defaults.Income),Expense:clean(v?.Expense,defaults.Expense)};localStorage.setItem(CATEGORY_KEY,JSON.stringify(data));return data}catch(e){return{Income:defaults.Income.slice(),Expense:defaults.Expense.slice()}}};
  const saveCats=data=>{try{localStorage.setItem(CATEGORY_KEY,JSON.stringify(data))}catch(e){}};
  const opts=(type,current='')=>{const cats=categoryData()[type]||[];return `<option value="" ${!current?'selected':''}>Select ${type.toLowerCase()} category</option>`+cats.map(c=>`<option value="${esc(c)}" ${c===current?'selected':''}>${esc(c)}</option>`).join('')+`<option value="__custom__">＋ Create custom category</option>`};
  window.openTransactionModal=function(id,type){if(typeof canAccess==='function'&&!canAccess('transactions'))return;const x=state.transactions.find(a=>a.id===id)||{type:type||'Income',date:today(),category:'',amount:'',description:''};const income=x.type==='Income';openModal(`<div class="entry-modal ${income?'entry-income':'entry-expense'}"><div class="entry-hero"><div class="entry-icon">${income?'＋':'−'}</div><div class="entry-title-wrap"><span class="entry-kicker">FINANCIAL ENTRY</span><h3>${id?'Edit':'New'} ${income?'Income':'Expense'}</h3><p>${income?'Record money received by the madrasa.':'Record money spent by the madrasa.'}</p></div><button class="entry-close" type="button" onclick="closeModal()">×</button></div><form onsubmit="saveTransaction(event,'${id?esc(id):''}')"><div class="entry-section"><div class="entry-section-title"><span>01</span><div><strong>Transaction details</strong><small>Choose the type and date</small></div></div><div class="entry-grid"><div class="entry-field"><label>Transaction Type</label><select class="entry-input" name="type" onchange="refreshEntryCategory(this.value)"><option value="Income" ${income?'selected':''}>Income</option><option value="Expense" ${!income?'selected':''}>Expense</option></select></div><div class="entry-field"><label>Date</label><input class="entry-input" type="date" name="date" value="${esc(x.date)}" required></div></div></div><div class="entry-section"><div class="entry-section-title"><span>02</span><div><strong>Money details</strong><small>Select the correct category and enter the amount</small></div></div><div class="entry-grid"><div class="entry-field"><label>${income?'Income':'Expense'} Category</label><div id="entryCategoryWrap"><select class="entry-input" id="transactionCategory" name="category" onchange="categoryChanged(this)">${opts(x.type,x.category)}</select></div><div class="entry-hint">Select a suitable category or create your own category.</div></div><div class="entry-field"><label>Amount</label><div class="amount-wrap"><span>₹</span><input class="entry-input amount-input" type="number" name="amount" value="${esc(x.amount)}" min="0.01" step="0.01" inputmode="decimal" placeholder="0.00" required></div></div></div></div><div class="entry-section"><div class="entry-section-title"><span>03</span><div><strong>Income / Expense Description</strong><small>Write what the money was received or spent for</small></div></div><div class="entry-field"><label>Details <em>Optional</em></label><textarea class="entry-input entry-note" name="description" maxlength="250" rows="4" placeholder="e.g. September student fee collection, rice purchase, teacher salary...">${esc(x.description)}</textarea><div class="entry-hint">A clear description makes future account reports easier to understand.</div></div></div><div class="entry-footer"><button type="button" class="entry-cancel" onclick="closeModal()">Cancel</button><button class="entry-save" type="submit"><span>${id?'Save Changes':income?'Save Income':'Save Expense'}</span><b>→</b></button></div></form></div>`) };
  window.refreshEntryCategory=function(type){const wrap=document.getElementById('entryCategoryWrap');if(wrap)wrap.innerHTML=`<select class="entry-input" id="transactionCategory" name="category" onchange="categoryChanged(this)">${opts(type,'')}</select><div class="entry-hint">Select a suitable category or create your own category.</div>`};
  window.categoryChanged=function(el){if(el.value!=='__custom__')return;const type=el.closest('form')?.querySelector('[name="type"]')?.value||'Income';const name=prompt(`Enter new ${type.toLowerCase()} category:`);if(!name||!name.trim()){el.value='';return}const cleanName=name.trim();if(cleanName.toLowerCase()==='normal'){alert('Normal category is not available.');el.value='';return}const cats=categoryData();if(cats[type].some(c=>c.toLowerCase()===cleanName.toLowerCase())){alert('This category already exists.');el.value='';return}cats[type].push(cleanName);saveCats(cats);refreshEntryCategory(type);const select=document.getElementById('transactionCategory');if(select)select.value=cleanName};
})();