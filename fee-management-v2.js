// Dingel Hafizia App — normalized Student Fees Management v2
(function () {
  'use strict';
  const MONTHS = [['APR','APRL',4],['MAY','MAY',5],['JUN','JUN',6],['JUL','JUL',7],['AUG','AUG',8],['SEP','SEP',9],['OCT','OCT',10],['NOV','NOV',11],['DEC','DEC',12],['JAN','JAN',1],['FEB','FEB',2],['MAR','MAR',3]].map(([code,label,number])=>({code,label,number}));
  const db=firebase.firestore();
  const esc=v=>String(v??'').replace(/[&<>\"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[m]));
  const money=v=>`₹${(Number(v)||0).toFixed(2)}`;
  const currentAcademicYear=()=>{const d=new Date(),y=d.getMonth()>=3?d.getFullYear():d.getFullYear()-1;return `${y}-${String(y+1).slice(-2)}`};
  const monthYear=v=>/^\d{4}-\d{2}$/.test(String(v))?String(v):`${new Date().getFullYear()}-${String(new Date().getMonth()+1).padStart(2,'0')}`;
  const academicYearForMonth=v=>{const[y,m]=monthYear(v).split('-').map(Number);return `${m>=4?y:y-1}-${String(m>=4?y+1:y).slice(-2)}`};
  function normalizeCategory(s){const raw=String(s.feeCategory||s.type||s.category||'STANDARD').trim().toUpperCase().replace(/[ -]+/g,'_');if(raw==='ATIM'||raw==='ORPHAN')return'ATIM';if(raw==='N_RSD'||raw==='NRSD'||raw==='NON_RESIDENTIAL')return'N_RSD';if(raw==='POOR_FREE')return'NO_FEE';return raw==='NO_FEE'?'NO_FEE':'STANDARD'}
  function feeFromStudent(s){const c=normalizeCategory(s);if(c==='ATIM'||c==='NO_FEE')return 0;return Math.max(Number(s.monthlyFee??s.monthlyFees??0)||0,0)}
  function statusFor(expected,paid){expected=Math.max(Number(expected)||0,0);paid=Math.max(Number(paid)||0,0);if(expected===0)return'FREE';if(paid===0)return'UNPAID';if(paid<expected)return'PARTIAL';return'PAID'}
  function calculatePayment(expected,paid){expected=Math.max(Number(expected)||0,0);paid=Math.max(Number(paid)||0,0);if(expected===0)return{expectedAmount:0,paidAmount:0,dueAmount:0,status:'FREE'};const safePaid=Math.min(paid,expected);return{expectedAmount:expected,paidAmount:safePaid,dueAmount:Math.max(expected-safePaid,0),status:statusFor(expected,safePaid)}}
  let state={students:[],structures:[],payments:[],transactions:[],month:monthYear(),year:currentAcademicYear()};
  async function loadData(){const[studentsSnap,structuresSnap,paymentsSnap,txSnap]=await Promise.all([db.collection('students').get(),db.collection('feeStructures').where('academicYear','==',currentAcademicYear()).get().catch(()=>({docs:[]})),db.collection('monthlyPayments').where('academicYear','==',currentAcademicYear()).get().catch(()=>({docs:[]})),db.collection('feeTransactions').where('academicYear','==',currentAcademicYear()).get().catch(()=>({docs:[]}))]);state.students=studentsSnap.docs.map(d=>({id:d.id,...d.data()}));state.structures=structuresSnap.docs.map(d=>({id:d.id,...d.data()}));state.payments=paymentsSnap.docs.map(d=>({id:d.id,...d.data()}));state.transactions=txSnap.docs.map(d=>({id:d.id,...d.data()}));window.dhFeeV2=state;render()}
  const activeStudents=()=>state.students.filter(s=>String(s.status||'Active').toLowerCase()==='active');
  const structureFor=s=>state.structures.find(f=>f.className===s.className&&f.category===normalizeCategory(s)&&f.academicYear===state.year);
  function expectedFor(s){const c=normalizeCategory(s);if(c==='ATIM'||c==='NO_FEE')return 0;const f=structureFor(s);return f?Math.max(Number(f.monthlyAmount)||0,0):feeFromStudent(s)}
  const paidFor=(id,month)=>state.transactions.filter(t=>String(t.studentId)===String(id)&&String(t.month)===month).reduce((sum,t)=>sum+Math.max(Number(t.amount)||0,0),0);
  function summary(month){let target=0,collected=0,due=0,paidStudents=0,partialStudents=0,unpaidStudents=0,freeStudents=0;activeStudents().forEach(s=>{const e=expectedFor(s),p=Math.min(paidFor(s.id,month),e);target+=e;collected+=p;due+=Math.max(e-p,0);const st=statusFor(e,p);if(st==='PAID')paidStudents++;else if(st==='PARTIAL')partialStudents++;else if(st==='UNPAID')unpaidStudents++;else freeStudents++});return{target,collected,due,paidStudents,partialStudents,unpaidStudents,freeStudents}}
  async function ensureMonthlyPayment(s,month){const e=expectedFor(s),p=Math.min(paidFor(s.id,month),e),c=calculatePayment(e,p),id=`${s.id}_${month}`;await db.collection('monthlyPayments').doc(id).set({paymentId:id,studentId:s.id,roll:s.roll||s.admissionId||'',studentName:s.name||'',academicYear:academicYearForMonth(month),month,expectedAmount:c.expectedAmount,paidAmount:c.paidAmount,dueAmount:c.dueAmount,status:c.status,updatedAt:firebase.firestore.FieldValue.serverTimestamp()},{merge:true})}
  async function collectFee(studentId,month,amount,paymentDate,note,method){const s=state.students.find(x=>String(x.id)===String(studentId));if(!s)throw new Error('Student not found.');const e=expectedFor(s),existing=paidFor(studentId,month),value=Number(amount);if(!Number.isFinite(value)||value<=0)throw new Error('Enter a valid payment amount.');if(e<=0)throw new Error('This student has no monthly fee.');if(existing+value>e)throw new Error(`Payment exceeds remaining due. Remaining: ${money(Math.max(e-existing,0))}`);const tx=db.collection('feeTransactions').doc(),receipt=`DHA-${tx.id.slice(-8).toUpperCase()}`;await tx.set({transactionId:tx.id,studentId:s.id,roll:s.roll||s.admissionId||'',studentName:s.name||'',academicYear:academicYearForMonth(month),month,amount:value,paymentDate:paymentDate||new Date().toISOString().slice(0,10),paymentMethod:method||'CASH',note:note||'',receiptNo:receipt,createdBy:window.dhAuth?.currentUser?.uid||null,createdAt:firebase.firestore.FieldValue.serverTimestamp()});await ensureMonthlyPayment(s,month);await loadData();return receipt}
  function rebuildPage(){const page=document.getElementById('page-fees');if(!page)return;page.innerHTML=`<div class="page-head"><div><h1>Fees & Monthly Collection</h1><p class="page-subtitle">Google Sheet structure — normalized, searchable and automatic</p></div><div class="fee-v2-actions"><button id="feeRefreshV2">↻ Refresh</button></div></div><div class="fee-v2-toolbar"><label>Academic Year <select id="feeYearV2"></select></label><label>Month <select id="feeMonthV2"></select></label><input id="feeSearchV2" type="search" placeholder="Search roll, student, phone..." autocomplete="off"><select id="feeStatusV2"><option value="">All Status</option><option value="PAID">Paid</option><option value="PARTIAL">Partial</option><option value="UNPAID">Unpaid</option><option value="FREE">ATIM / Free</option></select></div><div class="fee-v2-summary" id="feeSummaryV2"></div><div class="fee-v2-scroll"><table class="fee-matrix"><thead id="feeMatrixHead"></thead><tbody id="feeMatrixBody"></tbody><tfoot id="feeMatrixFoot"></tfoot></table></div><div class="fee-v2-note"><b>Logic:</b> ATIM/Free = ₹0 target and FREE status. Partial payments remain in transaction history. Paid, due and target are calculated from normalized records.</div><dialog id="feeCollectV2" class="modal"><div class="modal-body"><h2>Collect Fee</h2><div id="feeCollectInfo"></div><form id="feeCollectForm"><div class="field"><label>Payment Amount *</label><input id="feeAmountV2" type="number" min="0.01" step="0.01" required inputmode="decimal"></div><div class="form-grid"><div class="field"><label>Payment Date *</label><input id="feeDateV2" type="date" required></div><div class="field"><label>Method</label><select id="feeMethodV2"><option>CASH</option><option>UPI</option><option>BANK</option><option>OTHER</option></select></div><div class="field full"><label>Note</label><input id="feeNoteV2" maxlength="200"></div></div><div class="fee-live-calc" id="feeLiveCalcV2"></div><div class="modal-actions"><button type="button" id="feeCancelV2">Cancel</button><button type="submit" class="primary-btn">Collect Fee</button></div></form></div></dialog>`;const ys=document.getElementById('feeYearV2'),ms=document.getElementById('feeMonthV2');ys.innerHTML=`<option>${esc(state.year)}</option>`;ms.innerHTML=MONTHS.map(m=>`<option value="${state.year.slice(0,4)}-${String(m.number).padStart(2,'0')}">${m.label}</option>`).join('');ms.value=monthYear(state.month);document.getElementById('feeRefreshV2').onclick=loadData;ms.onchange=()=>{state.month=ms.value;render()};document.getElementById('feeSearchV2').oninput=renderMatrix;document.getElementById('feeStatusV2').onchange=renderMatrix}
  function renderSummary(){const s=summary(state.month),box=document.getElementById('feeSummaryV2');if(!box)return;box.innerHTML=[['Active Students',activeStudents().length],['Target / সর্ব মোট',money(s.target)],['Collected / জমা',money(s.collected)],['Due / বাকি',money(s.due)],['Paid',s.paidStudents],['Partial',s.partialStudents],['Unpaid',s.unpaidStudents],['ATIM / Free',s.freeStudents]].map(([l,v])=>`<div class="fee-stat"><b>${esc(l)}</b><strong>${esc(v)}</strong></div>`).join('')}
  function renderMatrix(){const head=document.getElementById('feeMatrixHead'),body=document.getElementById('feeMatrixBody'),foot=document.getElementById('feeMatrixFoot');if(!head||!body||!foot)return;const q=String(document.getElementById('feeSearchV2')?.value||'').trim().toLowerCase(),filter=document.getElementById('feeStatusV2')?.value||'';const rows=activeStudents().filter(s=>{const hay=[s.roll,s.admissionId,s.name,s.phone,s.guardianPhone,s.guardianName,s.className].join(' ').toLowerCase();if(q&&!hay.includes(q))return false;const st=statusFor(expectedFor(s),paidFor(s.id,state.month));return!filter||st===filter}).sort((a,b)=>String(a.roll||a.admissionId||a.name).localeCompare(String(b.roll||b.admissionId||b.name),undefined,{numeric:true}));head.innerHTML='<tr><th>ROL</th><th>STUDENTS</th><th>PH NO</th><th>FEES</th><th>APRL</th><th>MAY</th><th>JUN</th><th>JUL</th><th>AUG</th><th>SEP</th><th>OCT</th><th>NOV</th><th>DEC</th><th>JAN</th><th>FEB</th><th>MAR</th></tr>';body.innerHTML=rows.map(s=>{const e=expectedFor(s),feeLabel=e===0?(normalizeCategory(s)==='ATIM'?'ATIM':'FREE'):money(e),cells=MONTHS.map(m=>{const value=`${m.number<4?Number(state.year.slice(0,4))+1:Number(state.year.slice(0,4))}-${String(m.number).padStart(2,'0')}`,p=paidFor(s.id,value),st=statusFor(e,p),label=st==='FREE'?'FREE':st==='PAID'?'✓ PAID':st==='PARTIAL'?money(p):'—';return`<td><button class="fee-cell ${st.toLowerCase()}" data-student="${esc(s.id)}" data-month="${value}" title="${esc(st)}">${label}</button></td>`}).join('');return`<tr><td><b>${esc(s.roll||s.admissionId||'—')}</b></td><td><b>${esc(s.name||'—')}</b><small>${esc(s.className||'')}</small></td><td>${esc(s.phone||s.guardianPhone||'—')}</td><td><b>${feeLabel}</b></td>${cells}</tr>`}).join('')||'<tr><td colspan="16" class="empty">No active students found</td></tr>';const totals=MONTHS.map(m=>{const value=`${m.number<4?Number(state.year.slice(0,4))+1:Number(state.year.slice(0,4))}-${String(m.number).padStart(2,'0')}`;return{value,...summary(value)}});foot.innerHTML=`<tr class="summary-target"><th colspan="4">সর্ব মোট / TARGET</th>${totals.map(x=>`<td>${money(x.target)}</td>`).join('')}</tr><tr><th colspan="4">জমা হয়েছে / COLLECTED</th>${totals.map(x=>`<td>${money(x.collected)}</td>`).join('')}</tr><tr class="summary-due"><th colspan="4">বাকি আছে / DUE</th>${totals.map(x=>`<td>${money(x.due)}</td>`).join('')}</tr>`;body.querySelectorAll('.fee-cell').forEach(btn=>btn.onclick=()=>openCollect(btn.dataset.student,btn.dataset.month))}
  function openCollect(studentId,month){const s=state.students.find(x=>String(x.id)===String(studentId));if(!s)return;const e=expectedFor(s),already=paidFor(s.id,month),due=Math.max(e-already,0);if(e<=0){alert('This student is ATIM/Free. No monthly fee is required.');return}const modal=document.getElementById('feeCollectV2');document.getElementById('feeCollectInfo').innerHTML=`<div class="fee-collect-card"><b>${esc(s.name)}</b><span>Roll: ${esc(s.roll||s.admissionId||'—')}</span><span>Month: ${esc(month)}</span><span>Monthly Fee: ${money(e)}</span><span>Already Paid: ${money(already)}</span><strong>Remaining Due: ${money(due)}</strong></div>`;const amount=document.getElementById('feeAmountV2'),date=document.getElementById('feeDateV2');amount.value='';amount.max=due.toFixed(2);date.value=new Date().toISOString().slice(0,10);const calc=()=>{const entered=Math.max(Number(amount.value)||0,0),total=already+entered,newDue=Math.max(e-total,0);document.getElementById('feeLiveCalcV2').innerHTML=`New Total Paid: <b>${money(Math.min(total,e))}</b> &nbsp; Remaining: <b>${money(newDue)}</b> &nbsp; Status: <b>${statusFor(e,total)}</b>`};amount.oninput=calc;calc();document.getElementById('feeCancelV2').onclick=()=>modal.close();document.getElementById('feeCollectForm').onsubmit=async ev=>{ev.preventDefault();try{const receipt=await collectFee(s.id,month,Number(amount.value),date.value,document.getElementById('feeNoteV2').value,document.getElementById('feeMethodV2').value);modal.close();alert(`Fee collected successfully. Receipt: ${receipt}`)}catch(err){alert(err?.message||'Could not save fee payment.')}};modal.showModal()}
  function render(){rebuildPage();renderSummary();renderMatrix()}
  window.renderFees=function(){if(!document.getElementById('feeMatrixBody'))loadData();else render()};
  window.addPayment=function(){const first=activeStudents().find(s=>expectedFor(s)>paidFor(s.id,state.month));if(first)openCollect(first.id,state.month);else alert('No active student has an outstanding fee for this month.')};
  window.refreshFeeManagementV2=loadData;
  document.addEventListener('DOMContentLoaded',()=>loadData().catch(e=>console.error('Fee Management v2 load failed:',e)));
})();
